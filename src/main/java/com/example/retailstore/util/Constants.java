package com.example.retailstore.util;

import java.math.BigDecimal;

public class Constants {

	public static final BigDecimal EMPLOYEE_DISCOUNT_PERCENTAGE = BigDecimal.valueOf(0.3);
	public static final BigDecimal AFFILIATE_DISCOUNT_PERCENTAGE = BigDecimal.valueOf(0.1);
	public static final BigDecimal CUSTOMER_DISCOUNT_PERCENTAGE = BigDecimal.valueOf(0.05);
}
